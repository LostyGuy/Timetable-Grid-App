import 'package:flutter/material.dart';

class SupportMePage extends StatelessWidget {
  const SupportMePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Support Me Page'),
    );
  }
}